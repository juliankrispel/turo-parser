import nearley from 'nearley';
import parser from './parser';

export class Document {
  constructor(lines) {
    this.lines = lines;
  }

  updateStatement(index, text) {
  }

  getStatement(index) {

  }

  toString() {

  }
}
